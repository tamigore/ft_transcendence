// ./src/helper,js

export const server = {
    baseUrl: 'http://localhost:3000',
    // inetUrl: new URL(location.href).hostname,
    // baseUrl: "http://" + new URL(location.href).hostname + ":3000"
}